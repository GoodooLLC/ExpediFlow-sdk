name: expediflow_sdk
description: ExpediFlow Order Selling SDK - модуль для автоматизации работы экспедиторов
version: 1.0.0

environment:
  sdk: ^3.10.1
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  url_launcher: ^6.2.2
  intl: ^0.19.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^11.0.0
  connectivity_plus: ^6.0.0
  goodoo_pay: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mock/
